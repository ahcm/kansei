[package]
name = "kansei"
version = "0.0.3"
edition = "2024"
default-run = "kansei"
authors = ["Andreas Hauser <andreas@creative-memory.net>"]
description = "Kansei programming language"
license = "EUPL-1.2"
keywords = ["programming", "language"]
repository = "https://github.com/ahcm/kansei"

[workspace]
members = [
    "wasm/plotters",
    "wasm/spectral_norm",
]

[profile.release]
strip = true
#lto = "fat"        # Link Time Optimization: mostly for size benefits
#codegen-units = 1  # Reduce parallelism to allow better optimization
panic = "unwind"   # Keep runtime panic recovery consistent with development builds
opt-level = 3      # Maximize speed (default is 3, but good to be explicit)

[dependencies]
tempfile = "3.27"
directories = "6.0.0"
rustc-hash = "2.1.3"
rustyline = "17.0.2"
smallvec = "1.16.1"
wasmi = { version = "1.1.0", optional = true }
wasmtime = "41.0.4"
wat = "1.259.0"
wasmprinter = "0.243.0"
wit-component = "0.243.0"
wasi-preview1-component-adapter-provider = "=48.0.2"
polars = { version = "0.52.0", default-features = false, features = ["csv"] , optional = true }
clap = { version = "4.6.7", default-features = false, features = ["std"] , optional = true }
serde_json = "1.0.151"
regex = { version = "1.13.1", optional = true }
glob = "0.3.4"
chrono = { version = "0.4.45", default-features = false, features = ["clock"] , optional = true }
notify = "8.2.0"
libc = "0.2.189"
rand = { version = "0.9.5", optional = true }
rayon = "1.12.0"
sha2 = { version = "0.10.9", optional = true }
hmac = { version = "0.12.1", optional = true }
blake3 = { version = "1.8.7", optional = true }
ureq = { version = "3.4.2", default-features = true , optional = true }
csv = { version = "1.4.0", optional = true }
base64 = { version = "0.22.1", optional = true }
uuid = { version = "1.26.1", features = ["v4"] , optional = true }
toml = "0.9.12"
serde_yaml = { version = "0.9.34", optional = true }
flate2 = { version = "1.1.10", default-features = false, features = ["rust_backend"] , optional = true }
image = { version = "0.25.10", default-features = false, features = ["png"] , optional = true }
rusqlite = { version = "0.38.0", features = ["bundled"] , optional = true }
memmap2 = { version = "0.9.11", optional = true }
eframe = { version = "0.33.3", optional = true }
crossterm = { version = "0.29.0", optional = true }
ratatui = { version = "0.30.2", default-features = false, features = ["crossterm"], optional = true }
rustls = { version = "0.23.45", optional = true }
webpki-roots = { version = "1.0.9", optional = true }
russh = { version = "0.63.3", optional = true }
tokio = { version = "1.53.1", features = ["rt-multi-thread", "net", "sync", "time", "io-util"], optional = true }

[dev-dependencies]
wasmtime-wasi = { version = "=48.0.2", default-features = false, features = ["p1", "p2"] }

[features]
default = ["std-lib-all"]
std-lib-all = [
    "lib-base64",
    "lib-bytes",
    "lib-clap",
    "lib-crypto",
    "lib-csv",
    "lib-datetime",
    "lib-egui",
    "lib-flate2",
    "lib-http",
    "lib-image",
    "lib-math",
    "lib-mmap",
    "lib-net",
    "lib-path",
    "lib-polars",
    "lib-regex",
    "lib-russh",
    "lib-serde",
    "lib-sqlite",
    "lib-tui",
    "lib-tests",
    "lib-toml",
    "lib-uuid",
    "lib-yaml",
]
lib-base64 = ["dep:base64"]
lib-bytes = []
lib-clap = ["dep:clap"]
lib-crypto = ["dep:rand", "dep:sha2", "dep:hmac", "dep:blake3"]
lib-csv = ["dep:csv"]
lib-datetime = ["dep:chrono"]
lib-egui = ["dep:eframe"]
lib-flate2 = ["dep:flate2", "dep:base64"]
lib-http = ["dep:ureq"]
lib-image = ["dep:image", "dep:base64"]
lib-math = []
lib-mmap = ["dep:memmap2"]
lib-net = ["dep:rustls", "dep:webpki-roots"]
lib-path = []
lib-polars = ["dep:polars"]
lib-regex = ["dep:regex"]
lib-russh = ["dep:russh", "dep:tokio"]
lib-serde = []
lib-sqlite = ["dep:rusqlite", "dep:base64"]
lib-tui = ["dep:crossterm", "dep:ratatui"]
lib-tests = []
lib-toml = ["lib-serde"]
lib-uuid = ["dep:uuid"]
lib-yaml = ["dep:serde_yaml", "lib-serde"]
flate2-zlib = ["lib-flate2", "flate2/zlib"]
flate2-zlib-ng = ["lib-flate2", "flate2/zlib-ng"]
wasmi = ["dep:wasmi"]
egui = ["lib-egui"]
tui = ["lib-tui"]
